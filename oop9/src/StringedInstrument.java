public interface StringedInstrument {
    /**
     * Interfaces do not have a configurator,
     * and interfaces object cannot be created.
     * Interface by other classes
     * should be carried out.
     */
    void compression();
}
