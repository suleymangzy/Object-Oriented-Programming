public interface Instrument {
    /**
     * Even if the access specifiers and properties
     * of the methods of the interfaces are not written, the default
     * It is public abstract.
     * Therefore these methods
     * Being in public when performing
     * in the lower grades must.
     * Interfaces do not provide state.
     * (they do not have object variables because they cannot create objects and cannot be inherited).
     */
    void playAnInstrument();
}
