public interface WindInstrument {
    /**
     * Interfaces do not provide state.
     * There are no object variables because
     * they cannot create object and their implementation
     * cannot be inherited.
     */
    void breath();
}
