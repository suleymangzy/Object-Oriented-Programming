public class Main {
    public static void main(String[] args) {
        Student student = new Student();// An object named Student is created.
        /*
        The methods developed according to the attributes of the created object were prepared
        in line with the user's wishes, using object_name.method_name in the main method.
         */
        student.name_what();
        student.screen_write();
    }
}